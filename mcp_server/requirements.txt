# Tropelex MCP server dependencies.
#
# Kept in a dedicated venv (mcp_server/.venv), separate from Tropelex's own
# system-Python server — see README.md in this directory for why.
mcp>=1.28.0
httpx>=0.27.0
