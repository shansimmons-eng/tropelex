# Test-only dependencies for mcp_server — not needed to run the server itself.
# Install alongside requirements.txt: uv pip install --python .venv/bin/python -r requirements-dev.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
